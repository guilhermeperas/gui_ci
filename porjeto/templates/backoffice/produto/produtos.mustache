<div class="list">
    <h1>{{h1_text}}</h1>
    <table>
        <thead>
        <tr>
            <th>id</th>
            <th>Nome</th>
            <th>Valor</th>
            <th>Imagem</th>
            <th>Edicao</th>
        </tr>
        </thead>
        <tbody>
            {{#list}}
            <tr>
                <td>{{id}}</td>
                <td>{{nome}}</td>
                <td>{{value}}$</td>
                <td><img class="img" src="{{caminho}}thumbnails/{{img_path}}" alt="produto image"/></td>
                <td>
                    <a href="{{base_url}}produtos/edit/{{id}}">Editar</a>
                    <a href="{{base_url}}produtos/delete/{{id}}">Apagar</a>
                </td>
            </tr>
            {{/list}}
        </tbody>
    </table>
    <p>{{{links}}}</p>
    <a href="{{create_produto}}">Adicionar produto</a>

</div>